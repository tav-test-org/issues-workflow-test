name: Change issue status on unassign
on: 
  issues:
    types: 
      - unassigned

jobs:
  move-unassigned-issue-to-backlog:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/change-issue-status
        with:
          target-status: "Backlog"
          github-token: ${{ secrets.TEST_REPO_TOKEN }}

